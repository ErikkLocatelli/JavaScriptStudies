// primitivos (imutáveis): string, number, boolean, undefined, null, bigint, symbol;

// refêRENCIA (mutável) - array, object, function. 
//da pra dizer que os valores primitivos s]ao valores que apenas são copiados, já os valores por refência apontam para o mesmo local 
// na memória, desse modo, variam igual. 

