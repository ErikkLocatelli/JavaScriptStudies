// ? : substituto para if else único 

function rebaixou(pontos) {
    let isRebaixado = pontos >= 10 ? 'vivo' : 'rebaixado'
    return console.log(isRebaixado)
}

pontos = 10 
rebaixou(pontos)

//let ver = n1 > n2 ? 'banana' : 'potato'; //variável = parametro (condição vdd, consdiçã ofalsa)
//console.log(ver)